# v3.1 Verification

## compileall

Exit code:

```text
0
```

## Notes

Pytest should be run locally after installing dependencies:

```bash
pip install -r requirements.txt
pytest
```

v3.1 changes are focused on account UX and offline-first usage.
