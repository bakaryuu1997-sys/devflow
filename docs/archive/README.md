# Archived documentation

Historical per-version reports, self-evaluations, and verification notes
(v3.1 → v12.0). They are kept for provenance but are **not** referenced by the
application or the test suite. Active docs live one level up in `docs/`.
